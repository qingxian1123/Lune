# 复制本文件为同目录下的 keystore.properties 并填入实际值。
# keystore.properties 已被 .gitignore 忽略,绝不要提交进仓库。
#
# keystore 生成方式见 docs/android.md「发布签名」一节。
# storeFile 建议指向仓库外的绝对路径(正斜杠),例如:
#   storeFile=C:/Users/you/.android-keys/lune-release.keystore

storeFile=
storePassword=
keyAlias=lune
keyPassword=
