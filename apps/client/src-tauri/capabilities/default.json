{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Lune 默认权限能力,允许 shell open 与 WebSocket/HTTP 到服务端。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}